import styled from 'styled-components';

const SignInContainer = styled.div `
    margin: 0.5rem auto;
    padding: 0.5rem auto;
    width: 30rem;

    .button{
        ${'' /* margin: 0.7rem; */}
        padding: 0.7rem;
    }

    
`

export default SignInContainer